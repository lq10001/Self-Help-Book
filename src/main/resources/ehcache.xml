<?xml version="1.0" encoding="UTF-8"?>
<ehcache updateCheck="false" monitoring="autodetect" dynamicConfig="false">

    <diskStore path="java.io.tmpdir"/>

    <defaultCache
           maxElementsInMemory="1000"
           eternal="true"
           timeToIdleSeconds="3600"
           timeToLiveSeconds="3600"
           overflowToDisk="true"
           diskSpoolBufferSizeMB="1" />

    <!-- booktype -->
    <cache name="booktype" maxElementsInMemory="100"/>


    <!-- book -->
    <cache name="book" maxElementsInMemory="1000"/>

    <!-- borrower -->
    <cache name="borrower" maxElementsInMemory="1000"/>

    <!-- borrowbook -->
    <cache name="borrowbook" maxElementsInMemory="1000"/>

    <!-- returnbook -->
    <cache name="returnbook" maxElementsInMemory="1000"/>

    <!-- user -->
    <cache name="user" maxElementsInMemory="1000"/>

    <!-- menu -->
    <cache name="menu" maxElementsInMemory="1000"/>


</ehcache>